__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# local config overrides; configs/*.yaml themselves are tracked
configs/*.local.yaml
.env

# figures and tables written by validate.ipynb; drop this line to track them
figures/

# run outputs live under runs_dir, outside the repo, but guard anyway
runs/
*.pth
*.tsv
*.log
wandb/
